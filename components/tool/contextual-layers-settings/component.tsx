import React from 'react';

import { ContextualLayersSettingsProps } from './types';

export const ContextualLayersSettings: React.FC<ContextualLayersSettingsProps> = ({}: ContextualLayersSettingsProps) => (
  <div className="c-tool-contextual-layers-settings">
  </div>
);

export default ContextualLayersSettings;
